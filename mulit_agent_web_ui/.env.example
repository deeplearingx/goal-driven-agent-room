VITE_API_BASE_URL=
# Only for trusted local development. Do not embed a production API key in a browser build.
VITE_GATEWAY_API_KEY=
# Used only when GATEWAY_TRUST_TENANT_HEADER=true behind a trusted proxy.
VITE_TENANT_ID=
VITE_DEV_BACKEND_URL=http://localhost:8765
