# Ledger — agent-room 详细落地记录 (v0.1–v1.0)

> 从 [PLAN.md](../../PLAN.md) §0 拆出（HYG-1，2026-06-23）。PLAN §0 现为一页
> changelog；本文件保留每个里程碑的完整记录——测试数、独立性子模块数、设计权衡、
> 故意没做的取舍。大致按落地顺序排列。

---

## 0. 当前状态 (v0.4 §4.1-§4.8 + §3.4 spill 全部落地，2026-06-13；v0.4.x 真 LLM smoke 在 DeepSeek-V4-Pro 实测验证 §4.3 + §3.4，Q1/Q2 双绿；v0.4 真账本钉死；v0.5 §5.1-§5.6 跨会话记忆全部落地，2026-06-14，336/336 全绿；P3-C mypy strict + P3-D coverage 收口，2026-06-14，336/336 / 1918 stmts / 93%；P3-B v0.1 烟雾 carry-forward F1-F5 全部对账归档，2026-06-14；v1.0 §6.1 Vue UI 形状审计完成，2026-06-14；v1.0 §6.1 D1-D4 已拍板 + §6.1.1-§6.1.7 子任务卡入 §8，2026-06-22；v1.0 §6.1.1 sessions 薄壳 + §6.1.2 ADR-0011 + §6.1.3 `/healthz` role 映射落地，2026-06-22，367/367 全绿，独立性 46→48 子模块；**v1.0 §6.1 撤回 hermes-web-ui 复用，转向仓内薄 UI（ADR-0012），§6.1.4-§6.1.7 superseded by §6.1.4'-§6.1.6'，预算 ~2.5d，2026-06-23**；**v1.0 §6.1.4'-§6.1.6' 仓内薄 UI 全部落地，2026-06-23，373/373 全绿，独立性 48→50 子模块**；**v1.0 §6.2.x LLM transport 抽象（ADR-0013，借鉴 hermes-agent agent/transports/）落地，2026-06-23，379/379 全绿，独立性 50→52 子模块，6 处 role/llm 重复 wrap 收敛到一处**；**CLAUDE.md trim 落地：344→219 行（-36%），§5/§7/§9/§10 拆出到 docs/ARCHITECTURE.md / CONTRIBUTING.md / SECURITY.md，§3 通用规范压成 ruff 引用，2026-06-23**；**v1.0 重定向 → 作品集证明（新增"项目目标·北极星"节，砍生产化栈）；EVAL-1a eval harness 骨架落地：`agent_room/eval/{task,variant,runner,report}.py` 从 escalation_lab 泛化（oracle-agnostic + `Variant` 带 `bindings_factory` + 注入式 `graph_builder`），5 离线测试，384/384 全绿，独立性 52→57 子模块，2026-06-23**)

**v1.0 EVAL 进行中**（2026-06-23，简记 — HYG-1 后并入 changelog）：
- EVAL-1a 矩阵 harness 落地 `agent_room/eval/`（泛化自 escalation_lab，oracle-agnostic，graph 下沉到 variant×task 每对一次以支持沙盒 registry）+ `BehavioralTask` 重表达升级行为评分。
- EVAL-1b 正确性 oracle 落地 `CodingTask`（沙盒 seed + 工具 root 钉沙盒 + 跑 pytest 验 exit 0）+ `evals/` 任务套 3 个种子；关键 offline 守卫 `test_seed_tasks_start_red`（每个任务未修必须 RED）。
- 状态：**390/390 全绿，mypy strict 0 错（58 source），独立性 58 子模块**。
- EVAL-1c 真 LLM 验证（2026-06-23，DeepSeek-V4-Pro）：`evals/run.py` 入口 + `evals/variants.py` 工具化 react developer 变体；3 个种子任务真跑 **3/3 PASS、status=completed、token 采集生效（9013/7574/7567）**。loop 闭合证明。`snapshots/eval/summary.md` 第一张真实表。
- **run A 三个发现（WRITE-1 素材）**：(F1) 工具策略拒绝命令时**抛 ValueError 炸掉整个 run**，而非回灌错误给 LLM 重试——真 robustness 缺口，eval 第一次跑就抓到；暂以扩 allowlist（含 `python`）绕过。(F2) DeepSeek 思考模式拒绝 `with_structured_output` 的强制 tool_choice；reviewer 须用非思考模型（`AGENT_ROOM_REVIEWER_MODEL=deepseek-chat`），developer react（bind_tools=auto）思考模式 OK。(F3) 3 个种子任务全"易"→ baseline 100%，**不区分消融**；EVAL-2 前需补**难/欠规格**任务让弱配置会失败。
- **run B（隐藏测试 + 3 区分性任务，2026-06-23）**：CodingTask 加 `hidden_tests`（verify 时才写入沙盒，developer 看不到——HumanEval/SWE-bench 式区分器）；median/chunk/flatten 三任务真跑。两个深化发现：(F1 复现) `cd` 命令又炸 run（`python`→`cd`，**被拒命令中止整 run 是反复性 robustness 缺口，污染 ~1/3 run 遥测**）。(F3 加深) **3 个隐藏边界任务对 DeepSeek-V4-Pro 不区分**——强模型 + 自跑测试的 developer 把没崩的全 PASS；**结论：单一"编码正确性"任务无法区分全部 5 轴，需 per-axis 任务类型**（planner_gate→欠规格行为评分 / context→长任务 / memory→跨会话）。这重塑 EVAL-2。
- **F1 修复落地（2026-06-23）**：`graph.py` 给非 approval 的 react ToolNode 设 `handle_tool_errors=_tool_error_to_message`——被拒命令/任意工具错误回灌成 ToolMessage 让 developer 适配，不再中止 run；approval 分支不动（自己合成 deny ToolMessage）。离线确定性复现 `tests/test_react_tool_errors.py`（scripted dev 调被拒 `cd` → 不崩、续跑到 delivery）；真 LLM 确认 flatten 任务从 status=running 崩溃 → status=completed/approved。**394/394 全绿**，独立性 58 子模块。完整闭环：eval 发现 → 离线复现 → 修 → 双验证（WRITE-1 金句素材）。
- **§5.7 StreamingContextScrubber wiring 落地（2026-06-23）**：新增 `StreamEventFormatter`，真实 token stream 入口（FastAPI `POST /tasks/stream` + CLI `--stream`）按 task_id 持有 `StreamingContextScrubber`；`<memory-context>...</memory-context>` 即使跨 chunk 也只输出 `[memory-context redacted]`，未闭合 fence 在 flush 时丢弃，普通 partial 文本在流尾释放。`tests/test_events.py` 增 4 个离线用例覆盖完整 fence / split fence / partial 非 fence flush / unclosed fence drop。验证：`tests/test_events.py` 8/8 通过，ruff 通过，mypy 通过；API/UI lifespan 测试仍复现既有 startup timeout，失败发生在请求前，另行处理。
- **EVAL-1 任务集补齐到作品集规模（2026-06-23）**：`evals/coding_tasks.py` 从 6 个任务扩到 **12 个 CodingTask**，新增 whitespace / parse_bool / group_by_key / dedupe / deep_get / merge_intervals 六类小型真实任务，均带可见测试 + 隐藏边界测试；`tests/test_eval_coding.py` 增 suite-size guard。顺手修 `CodingTask.verify()`：子进程 pytest 设置 `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1`，避免外部 pytest 插件让绿色沙盒超时。验证：`tests/test_eval_coding.py` 16/16 通过（3.81s），ruff 通过，mypy 通过。
- **EVAL-2 首格落地：planner_gate 消融（2026-06-23，DeepSeek-V4-Pro + 结构化节点 deepseek-chat）**：`evals/escalation_tasks.py`（4 个 BehavioralTask：3 欠规格 + 1 control）+ `evals/variants.py` baseline/planner_gate 变体 + run.py suite 选择（coding|planner_gate）。**第一张真实消融表**（`snapshots/eval/planner_gate/`）：**baseline 1/4 (25%) vs planner_gate 4/4 (100%)**——北极星命题（可插拔架构=消融框架）用真实数据验证，一个设计选择有可量化大 Δ。**诚实 caveat**：(1) **N=1 噪声**——rate_limiter cell 跨两次跑出 fail(254s 超时空转)/pass(79s 干净升级)两种结果，**坐实 EVAL-2 的 N≥3 协议必要性**；表用干净跑。(2) **行为通过 ≠ 完成**——planner_gate/retry_helper 正确升级(esc=True→pass)但 resume 后仍 failed；BehavioralTask 评"问没问"，不评"最终成没成"。(3) baseline 欠规格不止"乱猜"：2/3 是 reviewer 修订空转到 failed，1/3 invented-and-shipped。(4) **token=n/a**——EVAL-1c 只覆盖 react/工具路径（AIMessage 进 dev_messages），纯 `full`/`planner_gate` 管道丢 usage（已知缺口）。
- **WRITE-1 落地（2026-06-23）**：[`docs/portfolio.md`](docs/portfolio.md) 技术叙事——planner_gate 25%→100% 消融 + F1 find-and-fix + 三个反直觉判断（frozen-snapshot / 压 prompt 不压 state / graph 按 pair 建）+ judgment-by-omission + 诚实 limitations。每个论点挂真实证据（文件/数字/测试）。面试可投。
- 剩：更多消融轴（context/memory，各需对应任务类型）、EVAL-1c 纯管道 token 缺口、HYG-1（§0 压成一页 changelog）。

**已交付**（v0.1 + v0.2）：
- ✅ 核心包结构（[agent_room/](agent_room/)）
- ✅ planner / developer / reviewer / delivery 4 角色节点 + planner-gate / two-call-review 变体
- ✅ LangGraph `StateGraph` + `AsyncSqliteSaver` 持久化
- ✅ `AgentRoomService.run/stream/resume/snapshot` 门面
- ✅ FastAPI + SSE 服务（`POST /tasks`、`POST /tasks/stream`、`GET /tasks/{id}`、`POST /tasks/{id}/resume`）
- ✅ Typer CLI（`run` / `serve` / `show` / `resume`，含 `--graph` 选 preset/路径）
- ✅ **63 个离线测试全绿** + ruff clean
- ✅ examples：`run_once.py` / `streaming.py` / `resume_after_user_decision.py` / `solo.py` / `escalation_lab/`
- ✅ ADR-0001…0008 全部 accepted；详细设计在 [docs/adr/](docs/adr/)
- ✅ ADR-0008 **GraphSpec 落地**：spec.py + 5 个预设（full / dev_review / solo / planner_gate / two_call_review）+ `build_from_spec`，老 API 100% 兼容
- ✅ **v0.2 §2.9 escalation 实验真实 LLM 实测**（DeepSeek-V4-Pro，3 个 under-spec 场景）：
  - baseline 0/3 (0%) — F2 完全复现
  - **planner_gate 2/3 (67%)** — 远超 ≥30% 目标，推荐方案
  - two_call_review 1/3 raw 33% / 1/1 排除 provider parser-error 后 100%
- ✅ 真实 LLM 烟雾测试 5 项发现（F1 已修 / **F2 已修** / F3 推迟 v0.4 / F4 数据已收集 / F5 烟雾脚本规避）
- ✅ **v0.3 §3.1 + §3.2 起步**：`agent_room/tools/` 4 个内置工具（read_text / write_text / glob / shell）+ ToolRegistry + 路径/命令安全 helper，35 个新测试，98/98 离线全绿，零 hermes import
- ✅ **v0.3 §3.5 落地**：developer ReAct 子图（agent ↔ ToolNode），`spec.tools` 激活，`max_dev_rounds` 收敛保护，13 个新测试，**111/111** 全绿，零 hermes import
- ✅ **v0.3 §3.3 落地**：`tools/policy.py` 三模式（read_only 默认 / unrestricted / approval 占位），`NodeSpec.tool_mode` 字段 + 验证，policy 在 `_resolve_tools_with_policy()` 单点过滤进 ToolNode + ReAct，过滤全空时显式报错（不静默降级），16 个新测试，**127/127** 全绿，零 hermes import
- ✅ **v0.3 §3.9 落地**：`examples/dev_runs_tests.py` —— 出口标准活证据。沙盒 + 故意 bug 的 calc.py + pytest，developer 节点 `tools=[glob, read_text, write_text, shell]` + `tool_mode: unrestricted` + `max_dev_rounds: 8`，真实 LLM（DeepSeek-V4-Pro / Volcano Ark）一次跑通：发现 bug → 改 `a - b` 为 `a + b` → 跑 `pytest` 验证 → reviewer 批准。8 个离线测试（spec / registry / sandbox / no-creds skip path），零 hermes import
- ✅ **v0.3 §3.8 落地**：[`docs/tools.md`](docs/tools.md) 工具开发指南（quick start + 4 内置工具 + 三种权限模式 + 自定义工具 + ReAct 循环预算 + 安全包络 + Reference 表）；同步把 [`docs/graph-spec.md`](docs/graph-spec.md) 关于 `tools` 的 v0.2 「forward-compat 暂未启用」描述改为 v0.3 已落地说明
- ✅ **v0.3 主线收官（2026-06-12）**：**170/170 离线测试全绿** + ruff clean + 零 hermes import；§3.6/§3.7 实质完成（35 + 16 测试散落于 §3.1/§3.3 落盘时）；§3.4 spill 推迟 v0.4 合 ADR-0009
- ✅ **v0.3.x P3-A 落地**：[`agent_room/llm/_retry.py`](agent_room/llm/_retry.py) `retry_on_parser_error` —— 仅对 `OutputParserException` 重试，默认 2 次（DeepSeek/Ark 空 `tool_use` 块实测 33% 触发率，2 次降到 ~11%）。已接入 reviewer / planner_gate / reviewer_two_call 三个 `with_structured_output` 调用点。10 个测试（passthrough / 一次重试 / 用尽重试 / 不吞 ValueError 子类 / 不吞 RuntimeError / attempts=1 短路 / attempts=0 拒绝 / async 路径 / 工厂闭包断言）
- ✅ **v0.3.x P2-A 落地（approval 模式真实化）**：[`agent_room/tools/_approval.py`](agent_room/tools/_approval.py) `make_approval_wrapper` —— 借助 `langgraph.types.interrupt()` 实现每个工具调用前 halt → 人审 → 恢复；`apply_policy("approval")` 改为 pass-through（不再 NotImplementedError），过滤动作改在 ToolNode 的 `awrap_tool_call` 钩子里；`service.resume(task_id, decision, at_node="tool_call")` 接受 string（`"approve"` / `"deny"`）或 dict（`{"action": "deny", "message": ...}`），多个并行 tool_calls 共享一个 verdict（per-call 列表推迟 v0.4）。`TaskResult.pending_tool_calls` 字段暴露 halt 信息。`docs/tools.md` 新增 Approval mode walk-through 子节。22 个测试（决策 token / 字典形 / 未知形保守拒绝 / 全流程 approve→tool 执行 / deny→synthetic ToolMessage / non-tool_call 拒绝 dict 入参）
- ✅ **v0.3.x P3-E 落地（独立性硬测）**：[`tests/test_independence.py`](tests/test_independence.py) + [`tests/_independence_driver.py`](tests/_independence_driver.py) —— 子进程里装一个 `MetaPathFinder` 拦截任何 `hermes_*` 导入，再 `pkgutil.walk_packages` 强制导入 `agent_room` 全部 32 个子模块，任何懒加载 / `importlib.import_module(...)` 计算名 / 经 `__getattr__` 触发的 hermes 引用都会立刻报错。`.github/workflows/ci.yml` 独立性 job 在源码 grep 之外新增一条具名步骤 `Runtime independence — block hermes_* and force-import every submodule`。**171/171** 离线测试全绿，§12.4 三条硬指标从此真正可执行（之前只靠 grep）
- ✅ **v0.4 §4.1 起步（ContextEngine 接口）**：[`agent_room/context/engine.py`](agent_room/context/engine.py) `ContextEngine` ABC（`should_compress` + `compress` 两阶段，借鉴 hermes-agent 形状）+ `NoOpContextEngine` 默认实现 + 基类内置 `apply()` 便利方法（`should_compress` → `compress`，恒返回新列表）。基类**不**强制 `max_tokens` / `protect_first_n` 构造参数（那是 windowed/summary 实现的细节，留给 §4.2/§4.3）。11 个测试（ABC enforcement / NoOp 行为 / apply 路由 / 幂等性 / 顺序保留 / 混合消息类型）。**182/182** 全绿，零 hermes import（独立性测试 32 → 34 子模块自动覆盖新增的 `agent_room.context*`）
- ✅ **v0.4 §4.2 落地（WindowedContextEngine）**：消息条数预算 + 头/尾保护窗口 + 中段截断 marker。`WindowedContextEngine(max_messages, protect_first_n=0, protect_last_n=6)` —— `should_compress` 在 `len > max_messages` 时返回 True；`compress` 保留前 N + 后 M 条，中段替换为单条 `HumanMessage("[context truncated: K messages omitted]")`。预算单位是**消息条数**而非 token（避免 tokenizer 依赖；token-aware 推到 §4.3 SummaryEngine 或 v0.4.x 扩展）。构造期硬不变量：`max_messages >= protect_first + protect_last + 1`，否则压缩结果会再次触发压缩（幂等违反）—— 直接 `ValueError`。Marker 选 `HumanMessage` 而非 `SystemMessage`（部分 provider 合并/限制多 System；marker 概念上是 user-side stream 的"此处省略"标记）。**ReAct 边界对齐**：朴素切片可能产生孤儿 `ToolMessage`（其 `AIMessage(tool_calls=...)` 父在被丢弃的中段）—— Anthropic API 直接拒绝。`compress()` 切片后从 tail 头部丢弃孤儿 ToolMessage、从 head 尾部丢弃悬挂的 AI(tool_calls)，宁可 tail 比 `protect_last_n` 略短也不让 provider 拒收。19 个测试（构造校验 / 边界 off-by-one / 各种 head-tail 配比 / 混合消息类型 / 幂等性 / 顺序保留 / **3 个 ReAct 对齐**）
- ✅ **v0.4 §4.5 落地（节点接入）**：[`RoleBindings.context_engine`](agent_room/config.py)（默认 `NoOpContextEngine`，零行为变化）+ [`developer_react.py`](agent_room/roles/developer_react.py) 在调 LLM 前 `await context_engine.apply(convo)`。**关键不变量**：引擎压的是 LLM-input，**不**是持久化 state —— `state["dev_messages"]` 在 LangGraph `add` reducer 下保持 append-only（审计/恢复需要全量），引擎只 bound 每轮发送给 LLM 的提示词。这是从 hermes-agent 学到的语义：压 prompt 不压 state。`_append_budget_note` 仍在压缩前应用（注释贴在最后一条 HumanMessage，必入 tail，模型看得到）。4 个集成测试（NoOp 默认透传 / Windowed 压 prompt 但 state 增量正确 / 压缩后 LLM 看到的全是合法 tool_call/tool_result 配对 / 初次进入 [System,Human] 不触发压缩）。**205/205** 全绿，零 hermes import
- ✅ **v0.4 §4.3 落地（SummaryContextEngine）**：[`agent_room/context/summary.py`](agent_room/context/summary.py) `SummaryContextEngine(WindowedContextEngine)` —— 把 §4.2 的「丢弃带 marker」升级到「小模型摘要 + 占位」。**关键设计**：subclass `WindowedContextEngine` 而非重写——head/tail 切片 + ReAct 边界对齐 + 构造硬不变量 + 幂等性都是同一套，只换 marker 生成。引擎里把 `WindowedContextEngine.compress()` 重构成 `_partition()` + 异步 `_build_marker()` 两个 hook：父类 `_build_marker` 返 `HumanMessage("[context truncated: N messages omitted]")`；`SummaryContextEngine._build_marker` 序列化中段 → 调 summarizer.ainvoke([SystemMessage(preamble), HumanMessage(serialized)]) → 包装成 `HumanMessage(SUMMARY_HEADER + body + SUMMARY_FOOTER)`。**失败语义**：summarizer 报错 / 返回空字符串 → 退化到父类的 count-only marker（warning log），绝不让单点摘要失败炸掉一整轮 ReAct——比 RuntimeError 强多了。**故意没做**：spill 接入（§3.4 仍 deferred 到 v0.4 RISK-9 / ADR-0009）、迭代式摘要（hermes 的 `_previous_summary` 链式更新；agent-room ReAct 通常 6-15 轮，多次压缩很罕见）、focus topic、token 计数。序列化 helper `_serialize_for_summary` 给每条消息打 role 标签 + tool_call 元数据 + per-message 1500 字符截断（spill 落地前的兜底）。12 个测试（gate 短路不调 LLM / 摘要文本入 marker / preamble 与序列化中段正确传给 summarizer / 自定义 preamble / summarizer 异常 fallback / 空响应 fallback / 空 middle 直接 fallback 不调 LLM / 幂等 / ReAct 对齐继承 / 父类构造硬不变量继承 / 序列化角色标签 + tool_call / 序列化字符上限）。**217/217** 全绿，零 hermes import（独立性 34 → 35 子模块）
- ✅ **v0.4 §4.4 落地（分层 prompt 装配器）**：[`agent_room/prompt/builder.py`](agent_room/prompt/builder.py) 的 `Layer(name, role, render)` + `PromptBuilder(layers).build(state)` —— 借鉴 hermes-agent `agent/prompt_builder.py`（10 层 + 150 行 if-else 模型路由 + 模块级缓存）压缩到本仓库需要的 5 层 + 零模块级状态 + 模型路由由 layer 工厂的调用方负责（`build()` 保持纯）。**关键设计**：`render(state) -> str | None`，`None`/`""`/纯空白都是 "skip"；连续同 role 的 layer 输出用 `\n\n` 合并成一条 `BaseMessage`，所以 5 层渲染出来还是 `[SystemMessage, HumanMessage]` 这两个的形状，跟历史 `_build_initial_messages` 的 wire-format 一字不差。preset [`developer_prompt_builder()`](agent_room/prompt/developer.py) 5 个 layer：identity (system) / plan_and_task (human) / user_directives (human) / review_feedback (human) / extra_context (human)。每个 layer 是 stateless 闭包，方便测试和 `extend()` 复用。`developer.py` + `developer_react.py` 两个角色节点的手写拼接全部替换为 `prompt_builder.build(state)` —— 13 个 ReAct 测试 + 4 个 context 集成测试一字不动，等价性通过 byte-level golden 测试 5 种状态形状钉住（minimum / directives / review-no-code / review-with-code / extra_context_keys）。**故意没做**：模型路由（hermes 的 gemini/gpt/codex 分支）、prompt 缓存（LangGraph 自己 deterministic replay，不需要）、token 计数（那是 ContextEngine 的活）。23 个新测试（13 个 builder 核心：Layer 校验 + builder 构造 + skip 语义 + 同 role 合并 + 跳过不破坏邻接 + extend；10 个 developer preset：5 种状态等价性 + 默认 SYSTEM_WITH_TOOLS + 旧 DEVELOPER_SYSTEM 可选 + layer 顺序稳定）。**240/240** 全绿，零 hermes import（独立性 35 → 38 子模块）
- ✅ **v0.4 §4.6 落地（长任务压缩端到端测试）**：[`tests/test_context_long_task.py`](tests/test_context_long_task.py) —— v0.4 真出口标准的可执行版本。**关键判断**：§4.5 的集成测试只验证了"engine 在合成的 22 条历史上跑一轮"，从来没在真正的多轮 ReAct 循环里跑到底；PLAN.md §4 出口标准原文要求 50 轮长任务能在固定预算内完成，这条标准之前是口头的。这次写了 4 个离线测试把它钉死：(1) **`test_50_round_react_with_windowed_engine_stays_under_budget`** —— 49 个 tool_call + 1 个 final-text 的 scripted LLM，手动驱动 node 50 次 + 用 `_simulate_tool_node` 模拟 ToolNode 的 `add` reducer 增量；每一轮 LLM 输入断言 `len ≤ max_messages`、无 orphan ToolMessage；末态 state["dev_messages"] = 101 条审计完整。(2) **`test_50_round_react_with_summary_engine_calls_summarizer_per_compression`** —— 同样 50 轮，换 `SummaryContextEngine` + `_ScriptedSummarizer`（直接 import 自 §4.3 测试，不重写）；断言 summarizer.calls 次数 = 真正出现 marker 的轮次数（既无跳调、也无重调）。(3) **`test_protect_window_messages_byte_equal_across_compressions`** —— 采样 10 轮，断言 head[0:2]（System + Human）跨压缩 byte-equal，钉住保护窗口语义。(4) **`test_state_grows_unbounded_under_compression`** —— 50 轮跑完 state 长度精确等于 101，证明引擎只压缩 LLM 输入、不动 state 审计 truth。**故意没走真 LangGraph 图**：图测试的 noise 大、定位问题难，直接驱动 `make_developer_react` 返回的 callable + `_apply_update`/`_simulate_tool_node` 两个手工 helper 模拟 LangGraph 的 add reducer + ToolNode，整个测试 < 0.4s 跑完。**故意没做**：真 LLM 实测（DeepSeek/Anthropic 联调，等 §4.7/§4.8 文档 + example 落地后再说）、§3.4 spill 接入（RISK-9 / ADR-0009 单独）。4 个新测试，**244/244** 全绿，零 hermes import（独立性仍 38 子模块）
- ✅ **v0.4 §4.7 + §4.8 落地（文档 + 离线 PoC）**：[`docs/context.md`](docs/context.md) 215 行设计说明（为什么存在 / ABC 契约 / 三个引擎逐个解释 / 选哪个的 rule of thumb / 集成点 / 测试不变量索引 / §3.4 spill 留尾）+ [`examples/context_compression.py`](examples/context_compression.py) 离线 PoC：30 轮合成 ReAct 循环跑三个引擎，逐轮打印 `state_msgs` / `llm_input` / `compressed?`，可视化 NoOp 上 LLM 看 60 条 vs Windowed/Summary 上稳定 plateau 在 7 条。**关键判断**：本来 §4.8 计划接 DeepSeek 真测一把，但把它推后了——离线 PoC 已经把"压 prompt 不压 state"的语义画给用户看了，真测属于「验证 wire 行为和 mock 一致」的事，等 v0.5 联调一起做更划算。**故意没做**：真 LLM 联调（推到 v0.5）、Windowed + Summary 的对比图（CLI 表格够用）、`docs/context.md` 加 ASCII 流程图（mermaid 在 GitHub 渲染好但终端阅读差，还是文字）。**244/244** 全绿（无新测试——文档和 example 都不该加测试，example 自己跑 `python -m examples.context_compression` 就是它的测试），零 hermes import
- ✅ **v0.4 §3.4 spill 接入 SummaryContextEngine 落地（2026-06-13）**：[`agent_room/tools/spill.py`](agent_room/tools/spill.py) `SpillStore` ABC + `InMemorySpillStore`（content-hashed、线程安全、幂等）+ `maybe_spill_tool_message` / `spill_messages` 两个纯函数 helper —— 把超过阈值（默认 4 KB）的 `ToolMessage` 替换成 `[spill:<sha256-12char> size=NNNNN] preview...` 占位符，`tool_call_id` / `name` 元数据完整保留。`SummaryContextEngine` 加可选 `spill_store=None` + `spill_threshold=4_000` 两个 keyword-only 构造参数：`None`（默认）走 v0.4 §4.3 既有路径完全不变；非 `None` 时在 `_summarize` 里序列化前先 `spill_messages(middle, store)` 把大 ToolMessage 拍扁，**摘要器 prompt 由原始 payload 大小限定 → 由占位符长度限定**，单次 30 KB stdout 摘要成本从 ~30K char 降到 ~600 char。**故意没做**：3-layer turn-aggregate budget（hermes 的 `enforce_turn_budget`，per-message 阈值已够覆盖现有用例）、`read_text` 协调（spill 取回是 v0.5 持久化层落地后的事，需要 `ref://` resolver）、disk/SQLite 后端（`InMemorySpillStore` 进程内够用，v0.5 跟跨会话记忆一起规划存储后端避免双套）、默认开启（`RoleBindings.context_engine` 默认 `NoOpContextEngine`，即使换成 `SummaryContextEngine` 也得显式传 `spill_store` 才生效——零行为改动）。**关键设计**：spill 放在 `agent_room/tools/` 而不是 `agent_room/context/`，因为它本质是 tool runtime 副产品，但消费者只有 `SummaryContextEngine`——developer LLM 实际承受得住 30 KB ToolMessage（provider 上下文窗口 ~200 K），只有摘要器 prompt 进了 dropped middle 才被挤；spill 的真正动机是**摘要器预算**而非通用截断。19 个新测试（13 个 spill 单元：store 往返/内容寻址幂等/不同 payload 分立/未知 ref None/sub-threshold 跳过/大 payload 替换且保留 call_id/占位符自身幂等/默认阈值边界/不变更入参/list-block content/`spill_messages` 只动 ToolMessage/返回新列表；6 个引擎集成：`spill_store=None` 等价 v0.4 §4.3 / 大 ToolMessage 在摘要 prompt 里被替换 / 完整 payload 可从 store 取回 / 小 ToolMessage 不被无谓 spill / `tool_call_id` 在序列化中段仍正确配对 / 引擎输出 marker 形状不被 spill 影响）。**263/263** 全绿，零 hermes import（独立性 38 → 39 子模块，新增的 `agent_room.tools.spill`）。[`examples/context_compression.py`](examples/context_compression.py) 加了 `_spill_demo()` 第 4 段对照 PoC：同一 `SummaryContextEngine` 跑两遍，每轮 11 KB 模拟 `pytest -v` 失败 dump，离线实测摘要器平均 prompt **24 277 → 8 167 字符（3.0x 缩减）**、最大 prompt **43 703 → 14 705 字符**、25 轮相同 payload `store_entries=1` 验证 content-hashed 去重。比例 < 文档里"~10x"的乐观估计是因为 `_serialize_for_summary` 已经先按 1500-char/msg 截断了一次（spill 落地前的兜底），spill 把 1500 char 进一步压到 ~250 char 占位符；真实场景里 raw payload 大于截断阈值越多，spill 收益曲线越陡
- ✅ **v0.4.x 真 LLM smoke 落地、DeepSeek-V4-Pro 实测验证 v0.4 §4.3 + §3.4（2026-06-13）**：[`examples/live_summary_smoke.py`](examples/live_summary_smoke.py) 接 `RoleBindings().resolve("delivery")` 拿配置好的 provider，跑两遍——`spill_store=None` vs `InMemorySpillStore()`——同一个 21-条 ReAct fixture（auth-refactor 调试场景，包含 ~5 KB 模拟 `pytest -v` 失败 dump），分别捕获摘要器 prompt 大小、summary 文本、`[spill:` 是否泄漏到 summary、elapsed。**两个问题**离线测套永远答不了，今天都答清了：(Q1) 真模型能不能从 `_serialize_for_summary` 给的 role-tagged + tool_call_id-annotated + 1500-char-truncated 输入下写出可用 summary——**能**：DeepSeek-V4-Pro 两轮都生成具体、有引文（"`pytest tests/ -v`"、"`src/auth/middleware.py`"、"`RuntimeError: missing AUTH_SECRET env var`"）、按 `DEFAULT_SUMMARY_PREAMBLE` 要求结构化的 prose summary（无 spill 667 chars、有 spill 453 chars），不是泛泛照搬。(Q2) 真模型遇到 `[spill:<sha256-12char> size=4803] preview...` 占位符会不会照搬——**不会**：两次 `placeholder_in_summary=False`，模型把 placeholder 当透明 token，从 357-char preview 里抽到症状（"2 failures"——实际 47 failed，因为 preview budget 在第 2 行 FAILED 处截断；这是 spill 的 fidelity-vs-budget 设计权衡，不是模型 bug，下游需要真实 47 数字得从 store 按 ref 取回，正是 §3.4 的设计意图）。**实测数据**：no_spill prompt 2539 chars / summary 667 chars / 5.8s；with_spill prompt 1465 chars / summary 453 chars / 5.6s；spill 缩减 prompt **1.7x**（fixture 偏小，只有 1 个 ToolMessage 过 2KB 阈值；`examples/context_compression.py` 25-轮 11KB-payload 的离线 PoC 测出 3.0x，跟这次匹配方向一致）。`store_entries=1` 验证 content-hashed 去重在真路径下也对。**配置发现**：DeepSeek 的 Anthropic-compatible Messages API 在 `https://api.deepseek.com/anthropic` 子路径下（不是根路径），`.env` 里 `ANTHROPIC_BASE_URL` 必须带 `/anthropic` 后缀，否则 ChatAnthropic 走根路径会拿 HTTP 404。`_extract_text` 助手处理 DeepSeek 这类 reasoning model 返回的 content-blocks 列表（`{type:'thinking',...} + {type:'text',...}`），把 `thinking` 段丢掉只取 `text`——比 `.content.strip()` 假设更鲁棒。**故意没做**：把 `_extract_text` 抬到 `agent_room` 公共 API（只有 example 用，留在 example 里更诚实，等第二个调用者出现再抽）；fixture 加更多 spill 触发点测 N>1 dedup 效果（已被离线 PoC 覆盖）；自动判分 summary 质量（人眼看就够，加 LLM-as-judge 是另一个项目）。**263/263** 仍全绿、独立性 39 子模块、ruff/format clean。v0.4 真账本钉死
- ✅ **v0.5 §5.1-§5.6 跨会话记忆全部落地（2026-06-14）**：6 个子任务、73 个新测试、5 个新模块、2 个文档、1 个 ADR、1 个端到端 demo——所有 v0.5 出口标准达成。**架构**（[`docs/adr/0010-memory-architecture.md`](docs/adr/0010-memory-architecture.md)）：两层 + 一工具 + 冻结快照。**两层**：(curated) [`agent_room/memory/curated.py`](agent_room/memory/curated.py) 维护 `<root>/memory/MEMORY.md`（2200 char cap）+ `USER.md`（1375 char cap），`\n§\n` 分隔条目，`add` / `replace(old_substring, new)` / `remove(substring)` 三操作走"读全文 → 切条目 → 写回 → 原子 rename"路径，所有写入先过 11-pattern 威胁扫描（`ignore previous`、`you are now`、`curl/wget` 远程、`bash -c`、`cat .env`、`.ssh/authorized_keys` 写入、bidi-override + zero-width Unicode 等），rejected 内容**返回给 LLM 当 tool result**（不是 raise）让它换措辞重试；(transcript) [`agent_room/memory/fts.py`](agent_room/memory/fts.py) `TranscriptStore` 在共享的 `agent_room.db` 里建 `agent_room_memory_messages` + 外部内容 FTS5 虚拟表（schema 跟 LangGraph checkpointer 的 `checkpoints/writes/blobs` 完全 disjoint），三个触发器（AI/AD/AU）按外部内容 FTS5 文档要求的 `'delete'` pseudo-command 模式同步；查询时双路径 [FTS5 MATCH + `snippet()`] 主路 + [LIKE + `substr(instr-40, 120)`] CJK fallback，FTS query 经 `_build_fts_query` 重写成 OR 表达式（drop ≥2-char alnum tokens、过 50 词英文 stopword 表）—— 这是 §5.6 真跑 demo 时发现的 load-bearing 修复：FTS5 默认 MATCH 是 AND-across-tokens，"now refactor the OAuth2 token handling" 被存为 "implementing OAuth2 token refresh..." 在 AND 下零命中，OR 重写下 OAuth2/token 任一命中即可。**一工具**（[`agent_room/memory/tool.py`](agent_room/memory/tool.py)）：`memory(action: add|replace|remove, target: memory|user, content, old_substring=None)` —— 单一 BaseTool 两轴 dispatch，`recall` 故意不暴露（auto-prefetch 每轮跑 [`agent_room/roles/developer_react.py`](agent_room/roles/developer_react.py) 的 `_inject_memory` hook 自动找，少一个工具 LLM 决策更稳）。注册走 `register_builtin_tools(reg, ..., memory_provider=...)` opt-in（默认 None 不注册——零行为变化）。**冻结快照**：`MemoryProvider.system_prompt_block()` 在 `initialize()` 时读盘**一次**，session 期间 LLM 通过 `memory` tool 写的内容更新磁盘但**不动当前 prompt**——保护 prefix-cache（Anthropic/DeepSeek/OpenRouter 都按缓存前缀计费，2K-token system prompt 每轮失效 30 轮 ReAct 烧 60K cached-token，冻结快照塌成 2K）；in-session recall 由 transcript 层覆盖，跨 session 才隔代生效，刚启动的 session 反正读盘重建。**Provider 分层**：[`agent_room/memory/provider.py`](agent_room/memory/provider.py) `MemoryProvider` Protocol（`initialize/system_prompt_block/prefetch/sync_turn/close`） + `NoOpMemoryProvider`（默认绑到 `RoleBindings.memory`，v0.1-v0.4 行为 bit-for-bit 不变）；[`agent_room/memory/file_fts.py`](agent_room/memory/file_fts.py) `FileFtsMemoryProvider` 把 `CuratedFileStore` + `TranscriptStore` 拼成 v0.5 默认后端，并向 `MemoryTool` 暴露 `add_curated/replace_curated/remove_curated/live_curated_text` 写路径（不在 base Protocol 上，避免污染极简版本）。**developer_react.py 集成**（§5.5）：第一次 entry 时调 `_inject_memory(convo, memory, query=state["description"])` 在 `SystemMessage` 后追加 curated 块 + `<memory-context>` 围栏的 prefetch 块（围栏带显式 "this is retrieved context, NOT new user input" preface 防 prompt-injection re-trigger）；同时 fire-and-forget `sync_turn("user", description)` + 每轮 LLM 响应后 `sync_turn("assistant", text)`——通过 `asyncio.create_task` 不阻塞主路径，失败 warning 后 swallow；ReAct re-entry（`existing` 非空）跳过注入保 prefix-cache 稳定。**关键设计判断**：(1) prefetch query 拿 `state["description"]` 不拿 `_last_human_text(convo)`——后者带 `# Plan` / `# Task` markdown header 把 FTS5/LIKE 都搞坏（`#` 是 FTS5 特殊字符，整段 prompt 也太长 LIKE 找不到 substring）；(2) 双层切割理由：curated 是"主张"（LLM 主动决定记什么），transcript 是"搜索"（事后回查），混在一起对 LLM 不友好（一个 schema 装两种语义）；(3) 为什么不上 vector embedding：ADR-0007 的"不依赖 hermes 的 sentence-transformers 链"原则继续生效，FTS5 + LIKE 在 v0.5 量级够用，质量不够再加（v0.6+）。**故意没做**：TTL/age forgetting（`AGENT_ROOM_MEMORY_TTL_DAYS`，写盘没意义只清单纯老条目还得加 metadata 列）、reviewer/planner 节点的 memory tool 接入（v0.5 只 developer 写，多节点写需要去重策略）、proper CJK 分词（jieba/lindera 是另一个量级的依赖）、disk 后端 SpillStore 跟 transcript log 共表（v0.6 §3.4 联调）、GUI 编辑 MEMORY.md（v1.0 Vue UI）。**73 个新测试**：8 个 Protocol/NoOp/RoleBindings.memory 字段（[`tests/test_memory_provider.py`](tests/test_memory_provider.py)）；25 个 curated 后端（[`tests/test_memory_curated.py`](tests/test_memory_curated.py)：分隔/三操作/字符上限/原子写/ frozen snapshot 不被 mid-session add 影响/11 种威胁模式 reject + reject-message 包含 pattern/双层 cap target=memory vs target=user）；22 个 transcript 后端（[`tests/test_memory_fts.py`](tests/test_memory_fts.py)：append/search/snippet 标记/LangGraph 共库无冲突/三个触发器（INSERT/DELETE/UPDATE）按 'delete' pseudo-command 同步/CJK LIKE fallback/special chars 不 crash/multi-word OR 重写多次跑路 vs 共词/stopword-only 不全命中）；11 个 memory tool（[`tests/test_memory_tool.py`](tests/test_memory_tool.py)：action×target dispatch/`replace` 缺 old_substring/threat reject/oversize reject/registry opt-in 默认关/memory_provider 显式开后 ToolEntry 形状）；7 个 developer_react × Memory 集成（[`tests/test_memory_developer_react.py`](tests/test_memory_developer_react.py)：NoOp 不动 system prompt / curated 注入 / `<memory-context>` 围栏注入 / prefetch query 是 description 不是渲染过的 HumanMessage / sync_turn 在 user-query + assistant-response 各调一次 / ReAct re-entry 不重新注入 / 真 FileFtsMemoryProvider 跨 session 注入到下一个 dev_react 的 SystemMessage）。**端到端 demo**（[`examples/memory_recall.py`](examples/memory_recall.py) 209 行）：session 1 调 `memory` tool 写 user-pref + project-fact + 跑 dev_react 让 transcript 记下 "Implementing OAuth2 token refresh now using requests-oauthlib"，关 provider；session 2 全新 provider 同 root_dir/db_path → 4 项检查全 True：curated user pref 出现、curated project fact 出现、`<memory-context>` 出现、prior OAuth2 conversation 出现——**v0.5 出口标准 #1（"第一个任务里 LLM 调 remember(...)，第二个独立任务 LLM 能 recall 到"）通过实测**。**两个文档**：[`docs/adr/0010-memory-architecture.md`](docs/adr/0010-memory-architecture.md)（六大决策 + 替代方案否决理由——尤其驳回 single-layer transcript / single-layer curated / vector embeddings / 不冻结）+ [`docs/memory.md`](docs/memory.md) 190 行用户视角说明（数据流图 + tool 用法示例 + 4 行 wire-up 代码 + 威胁扫描清单 + CJK 局限说明 + v0.6+ 缺口）。**336/336** 全绿（v0.4 263 + v0.5 73），ruff/format clean，独立性 39 → 46 子模块（新增 `agent_room.memory.{provider,curated,fts,prompt,file_fts,tool}`），出口标准 #2（"全程不依赖任何外部数据库 / 向量库 / hermes-agent"）**通过**——`grep -rE "from hermes_|import hermes_" agent_room/memory/ tests/test_memory*.py` 零结果
- ✅ **P3-C mypy 严格模式 + P3-D coverage 报告（2026-06-14）**：把"工程地基"两块短板一次收口。**P3-C**：[`pyproject.toml`](pyproject.toml) 加 `[tool.mypy] strict=true` + `warn_unused_ignores` + `warn_redundant_casts` + `ignore_missing_imports`，`exclude=^(tests|examples|benchmarks|scripts)/` 暂排掉测试/示例（留给 v1.0 之前补）；产品代码（`agent_room/` 46 文件）从基线 50 错修到 0 错，主要 pattern：节点函数返回 `dict[str, Any]`、`StateGraph[TaskState, Any, TaskState, TaskState]` 全四参写死、`Command[Any]`、langchain `BaseChatModel` 工厂里 `cast()` 规避 stubs Any 漏出。三个边界文件（`agent_room.graph` / `agent_room.service` / `agent_room.config`）单独 `disable_error_code = ["arg-type","call-overload","call-arg"]`，三个工具子类（`agent_room.tools.fs` / `agent_room.tools.shell` / `agent_room.memory.tool`）单独 `disable_error_code = ["misc"]` 让 Pydantic v2 model field 子类赋值能过——这两组**只对边界文件**关，不污染主代码 strict 严签。**P3-D**：`pytest-cov>=5` + `coverage>=7.6` 进 `[project.optional-dependencies] dev`；`[tool.coverage.run]` 用 `source=["agent_room"]`；`[tool.coverage.report]` 设 `fail_under=90`（基线 93%，留 3 个百分点缓冲，新模块进来不会立刻顶到红线），`exclude_also` 覆盖 `if TYPE_CHECKING:` / `raise NotImplementedError` / `@(abc\.)?abstractmethod` / `pragma: no cover` 四种"不必算的代码"；CI [`.github/workflows/ci.yml`](.github/workflows/ci.yml) 在 lint-and-test job 的 3.13 矩阵里加一步跑 `pytest --cov=agent_room --cov-report=term-missing --cov-report=xml --cov-report=html`，再上传 `coverage.xml` + `htmlcov/` 成 `coverage-report` artifact 留 14 天（不上 codecov.io 第三方服务保隐私 + 独立性原则——任何人下 artifact 自己看就行）。`.gitignore` 加 `.coverage` / `.coverage.*` / `htmlcov/` / `coverage.xml`。README 加 CI / coverage / Python 三个 badge + Coverage 章节给出三种 reporter 命令 + 阈值说明。**基线指标**：336 测试 / 1918 stmts / 140 miss / **93%**——agent_room 共 38 个被测模块覆盖率分布：100% 18 个、≥95% 13 个、85-95% 6 个、最低 60%（`agent_room/config.py` 因 LLM 工厂分支需要真 provider key 才能跑到，离线测套碰不到）。**故意没做**：把 `config.py` 抬到 90%（要么 mock provider 工厂——不诚实，要么真 LLM smoke——v0.4.x 已经覆盖这条路径只是没接 `--cov` 收集，多此一举）；codecov.io / sonar 第三方集成（artifact 已经够用，第三方服务跟独立性原则有冲突）；分支覆盖（`branch=false` 因为目前分支结构以条件 dispatch 为主，stmt 覆盖已能体现，`branch=true` 会把基线降到 ~85% 触发 `fail_under` 调整链）；CI 三个 Python 都跑 `--cov`（重复 + 慢，3.13 一份够了）。**336/336** 仍全绿，mypy 0 错，ruff clean，coverage 93% ≥ 90% floor 通过
- ✅ **v1.0 §6.1.1 + §6.1.2 + §6.1.3 落地（2026-06-22，Vue UI 接力第 1+2 步）**：D1-D4 拍板（详见 §6.1）后，落地服务端 sessions 薄壳、ADR-0011 状态映射文档、`/healthz` 暴露 role→model 映射。**§6.1.1 sessions 薄壳**：[`agent_room/server/sessions.py`](agent_room/server/sessions.py) 122 行 `SessionStore` —— 三列表 `agent_room_sessions(id, name, created_at)` + 链表 `agent_room_session_tasks(session_id, task_id, attached_at)` 跟 LangGraph checkpointer / memory 完全 disjoint，独立 `aiosqlite` 连接，WAL + busy_timeout=1000 + foreign_keys=ON 跟 `TranscriptStore` 同款；CRUD 6 操作（create / get / list_all / delete / attach_task / list_tasks）+ `new_session_id()` uuid 工厂 + `SessionRecord` / `SessionTaskLink` frozen dataclass；session 删除 `ON DELETE CASCADE` 自动清链表，attach_task 用 `INSERT OR IGNORE` 幂等，list 都按 created_at/attached_at desc 排（前端"最新优先"语义）。[`agent_room/server/compat_v1.py`](agent_room/server/compat_v1.py) 134 行 `/api/agent-room/*` 路由（7 个）：`GET/POST /sessions`、`GET/DELETE /sessions/{id}`、`GET /sessions/{id}/tasks`、`POST /sessions/{id}/tasks`（透传 `service.run` 同时 attach）、`POST /sessions/{id}/tasks/{tid}/resume`（透传 `service.resume`）；session 不存在前置 404；`POST /sessions` 拒绝空名 422；resume body `decision` 非空字符串校验。`create_app` 拓 `session_store` 关键字参数（默认在 `service` test 路径用 `:memory:`，prod 路径用 `settings.db_path`）+ `lifespan` 拥有/释放 store；主路径 `/tasks` `/tasks/stream` `/tasks/{id}` `/tasks/{id}/resume` 完全不动（API 冻结友好）。**§6.1.2 ADR-0011**：[`docs/adr/0011-vue-ui-state-mapping.md`](docs/adr/0011-vue-ui-state-mapping.md) 钉死六件事：(1) sessions 表 schema = `(id, name, created_at)` 三列，无 lifecycle / metrics / config / role_bindings / runs / messages；(2) 12-状态 UI 标签 → 4-状态服务端 `TaskResult.status` + `Event[]` + `ReviewerDecision` 派生表（一向映射，前端 `if/else`）；(3) SSE 主路径，polling 弃用；(4) RoleBindings 留 v1.0 dataclass+env，UI 通过扩展 `/healthz` 只读；(5) 不迁移视图（`AgentRoomRunsView` / `AgentRoomRoleBindingModal` / `AgentRoomSessionMetrics*`）；(6) 迁移视图（`AgentRoomChatView` / `MessageList` / `TaskPanel` / `ArtifactsView` / `TimelineView` 改 Event[] 派生 / office scene / `CreateTaskModal` / `ReviewDecisionModal`）。**§6.1.3 `/healthz` role 映射**：[`agent_room/config.py`](agent_room/config.py) `RoleBindings.model_ids()` —— 对每个 role（planner/developer/reviewer/delivery）按 `resolve()` 同样的优先级返回 model id：显式 binding 走 `_describe_bound_model`（`model_name`/`model_id`/`model` attr 探测 + `<bound>` fallback），否则取 `Settings.role_models[role]` → `Settings.default_model`。`/healthz` 返回 `{"ok": True, "role_bindings": {...}}`，不暴露任何 `anthropic_auth_token` / `anthropic_api_key` / `openai_api_key`（4 个测试钉住：fallback / role_models override / explicit bound / 无 secret 泄漏）。**故意没做**：sessions 长 messages 表（每个 task 自带历史走 LangGraph state）、autoDeliveryEnabled 开关（delivery 节点是必经的）、session.metrics（前端不该独占指标，Prometheus 是服务端层面）、把 `model_ids()` 写进 `MemoryProvider` Protocol（`/healthz` 是 ops 视图，不是 LLM 视图）。**31 个新测试**：[`tests/test_sessions.py`](tests/test_sessions.py) 14 个（DAO CRUD / 持久化跨 reopen / 跟 checkpointer 表隔离 / cascade / 幂等 / 排序 / 空名拒绝 / frozen dataclass）+ [`tests/test_compat_v1.py`](tests/test_compat_v1.py) 13 个（路由 happy / 404 / 422 / resume / 主路径不受影响）+ [`tests/test_role_bindings_model_ids.py`](tests/test_role_bindings_model_ids.py) 4 个（fallback / role_models / explicit binding / 无 secret 泄漏）。**367/367** 全绿（v0.5 336 + v1.0 31），ruff/format clean，mypy strict 0 错（48 source files），独立性 46 → 48 子模块（`agent_room.server.{sessions,compat_v1}`）。
- ✅ **v1.0 §6.1.4' + §6.1.5' + §6.1.6' 落地（2026-06-23，UI 接力转向仓内薄 UI）**：[ADR-0012](docs/adr/0012-in-repo-thin-ui.md) 撤回 hermes-web-ui 复用方案后，落地仓内 server-rendered 薄 UI 一次性闭环。**§6.1.4' templates + JS**：[`agent_room/server/ui/`](agent_room/server/ui/) 新增 5 个文件——`templates/{base,index,task}.html` Jinja2（depth-1 inheritance，单一 `<style>` block 内联 `prefers-color-scheme` 双主题），`static/app.js` 单文件 ES2020（~150 行：index 页 form 提交 + task 页 `EventSource('/tasks/{id}/stream')` 订阅 + ADR-0011 §2 `deriveStatus(server, ev, review)` 4→12 状态派生 + resume 表单提交）；零 npm，零构建，零打包步骤；`pip install -e .` 即包含 UI。**§6.1.5' FastAPI routes**：[`agent_room/server/ui/routes.py`](agent_room/server/ui/routes.py) `build_ui_router(app)` 挂 `GET /ui` `GET /ui/tasks/{id}` 两页 + `GET /tasks/{id}/stream` EventSource 友好流变体（POST stream 保留），快照不存在前置 404；[`agent_room/server/api.py`](agent_room/server/api.py) `app.include_router(build_ui_router(app))` + `app.mount("/ui/static", StaticFiles(...))` + `GET /` 307 重定向 `/ui`（`include_in_schema=False` 不污染 OpenAPI）；[`pyproject.toml`](pyproject.toml) 加 `jinja2>=3.1` 依赖 + `[tool.hatch.build.targets.wheel.force-include]` 钉死 `templates/` + `static/` 进 wheel（避免 sdist/wheel 漏 non-Python 资源）。**§6.1.6' smoke + reconciliation 收尾**：[`examples/ui_smoke.md`](examples/ui_smoke.md) 五分钟走查（启动 → 新建 task → SSE 状态机 → human-in-the-loop resume → 独立性验证 `ls hermes-web-ui` 缺席仍可跑）；[`docs/v1.0-vue-ui-reconciliation.md`](docs/v1.0-vue-ui-reconciliation.md) 末尾加"2026-06-23 closed by ADR-0012"小节 + [`docs/v1.0-vue-ui-reconciliation_addendum.md`](docs/v1.0-vue-ui-reconciliation_addendum.md) 撤回原因 + 保留物清单；[`README.md`](README.md) Features 加 `/ui` 入口 + HTTP 段加 Browser 子段。**ADR-0011 不变**：12→4 状态映射表是契约，仓内 UI 沿用同一份；`/api/agent-room/*` 兼容面保留给自建 SPA。**故意没做**：npm/Vite/打包链路、Vue/React 框架、UI 端 polling 路径、UI 端 sessions 列表（task 是根资源）、auth/multi-user 门面（推 v1.x ingress 层）。**6 个新测试**：[`tests/test_ui_routes.py`](tests/test_ui_routes.py) 6 个（root 307 → /ui / index HTML 渲染 / task 页 task_id 注入 / `/ui/static/app.js` 静态资源 + JS 内 `EventSource` + `deriveStatus` 钉住 / GET stream replay 完整任务 task_started→task_finished 序列 + delivery / unknown task 404）。**373/373** 全绿（v1.0 §6.1.1-§6.1.3 367 + UI 6），ruff/format clean，mypy strict 0 错（**50** source files），独立性 48 → **50** 子模块（`agent_room.server.ui.{__init__,routes}`），coverage 92.57%（≥ 90 floor）。**剩余 v1.0**：API 冻结 + SemVer release / Postgres checkpointer 选项 / Prometheus + structlog / OpenTelemetry / docstring 完整化 / 独立性 CI step / PyPI 打包。
- ✅ **v1.0 LLM transport 抽象落地（2026-06-23，[ADR-0013](docs/adr/0013-llm-transport-abstraction.md)，hermes-agent 借鉴）**：撤回 CLAUDE.md §1 旧"非目标 不做 LLM provider 抽象"规则，引入有规模上限的 transport 接口（≤ 3 文件 / ≤ 300 行 / ≤ 2 实现）。**为什么开窗**：(a) `agent_room/llm/_retry.py::retry_on_parser_error` 已经在 reviewer / planner_gate / reviewer_two_call 里手工 wrap 了三处 + reviewer_two_call 自己里又两处共四处，新结构化 call 上来就要再补一次；(b) v1.0 即将进的 prompt-cache headers / reasoning_effort / Gemini thought_signature 都是 transport 层关心；(c) reviewer 协议本身就是 transport-shaped 契约；(d) hermes 89 行 `agent/transports/base.py` 已经把"convert_messages → convert_tools → build_kwargs → normalize_response"四方法的 shape 在 5 个真实 provider 上验证过，借形不借量。**[`agent_room/llm/transport.py`](agent_room/llm/transport.py)**：`Transport(Protocol)` 三方法 `invoke(role, msgs, *, tools=None, model_override=None) → NormalizedResponse` / `structured(role, schema, msgs, *, attempts=2, model_override=None) → BaseModel` / 流走 graph 的 `astream_events`（ADR-0001）所以 transport v1.0 不暴露 stream，加是 strictly additive 等真实消费者出现；`NormalizedResponse(message: AIMessage, usage: Usage|None, provider_data: dict|None)` 跟 hermes `types.py` 同形，`message` 直接挂 LangChain `AIMessage` 让现有节点读 `.tool_calls` / `.content` 不用翻译；`Usage(prompt/completion/total/cached_tokens)` 从 `AIMessage.usage_metadata` 抽（best-effort，不抓也不挂）；`provider_data` 是 opaque dict 留给未来 Gemini thought_signature / Anthropic reasoning_details 这种 cross-provider extras 的避难所。**`LangChainTransport`** = `RoleBindings.resolve` + LangChain 调用，`structured` 内部自动 `with_structured_output(schema) + retry_on_parser_error(attempts=...)` 一次到位——三处手工 wrap 收敛到一处。**节点改造（6 文件机械替换）**：[`reviewer.py`](agent_room/roles/reviewer.py) / [`planner.py`](agent_room/roles/planner.py) / [`developer.py`](agent_room/roles/developer.py) / [`delivery.py`](agent_room/roles/delivery.py) / [`planner_gate.py`](agent_room/roles/planner_gate.py) / [`reviewer_two_call.py`](agent_room/roles/reviewer_two_call.py) / [`developer_react.py`](agent_room/roles/developer_react.py) 七个 factory 都加 `transport: Transport | None = None` 默认参，省略时 `LangChainTransport(bindings)` 即时构造，业务代码 `await tx.invoke(role, msgs, ...)` / `await tx.structured(role, Schema, msgs, ...)` 替换原 `bindings.resolve(role).ainvoke(...)` / `with_structured_output + retry_on_parser_error` pattern。`developer_react` 把 `bind_tools(tools)` 移到 transport 内（`tools=` kwarg 触发），no-tools 路径不调 `bind_tools`（保 prefix-cache）。**CLAUDE.md 三处更新**：§1 非目标"不做 LLM provider 抽象"删掉，替换为"目标内：轻量 LLM provider 抽象（≤ 3 文件 / ≤ 300 行 / ≤ 2 实现，超规模需另立项）"；§2 禁止行从"直接用 OpenAI/Anthropic SDK 绕过 LangChain"换成"不经过 transport 直接调 `BaseChatModel`"；§4.5 加"业务代码读 `transport.invoke(role,...)` / `transport.structured(role,...)`，transport 内部代理 `bindings.resolve`"。**故意没做（明确 deferred）**：per-provider native transports（Anthropic/OpenAI 直接 SDK）—— LangChain 没瓶颈不写；token-level prompt cache control —— 等 Prometheus 上线找瓶颈；streaming `tool_call` deltas in `StreamChunk`——v1.0 只 emit text deltas，真消费者出现再扩；YAML provider catalog (hermes `plugins/model-providers/*/plugin.yaml`)——我们一个 env var per role，不是 hermes scale 需要。**6 个新测试** [`tests/test_llm_transport.py`](tests/test_llm_transport.py)：(1) `invoke` 返回 `NormalizedResponse(message=AIMessage)` raw passthrough；(2) `structured` 走 `with_retry`（spy `with_retry` 调用看 `RunnableRetry` 出现）；(3) `structured` 在第一次 `OutputParserException` 后重试到第 2 次成功；(4) `tools=[fake_tool]` 触发 `bind_tools` 恰好 1 次；(5) `model_override="my-model"` 流到 `bindings.resolve(role, model_override=)`；(6) 默认无 tools 路径 `bind_tools` 0 次（prefix-cache 保护）。**1 个测试改造** [`tests/test_llm_retry.py::test_reviewer_factory_wraps_structured_output`]：原检测 factory closure 里的 `RunnableRetry`（旧 wrap-at-factory pattern），改成 spy `with_retry` 调用确认 `transport.structured` 实际触发——契约从"factory 里有 retry 实例"变成"每次 structured call 都过 retry"，更贴本质。**379/379** 全绿（373 + 6 transport），ruff format/check clean，mypy strict 0 错（50→**52** source files），独立性 50 → **52** 子模块（`agent_room.llm.transport` + `agent_room.memory.scrubber` 也带进——见 §5.7 待办）。**剩余 v1.0**：scrubber wiring（§5.7）/ API 冻结 + SemVer release / Postgres checkpointer 选项 / Prometheus + structlog / OpenTelemetry / docstring 完整化 / 独立性 CI step / PyPI 打包。

**已知缺口（带入 v0.3+）**：
- ⚠️ Reviewer 节点的 `with_structured_output` 在 OpenAI 模型下需要不同模式
- ⚠️ 长任务的 token 流式可能产生过多 SSE 帧，需要节流
- ⚠️ DeepSeek-V4-Pro 偶发返回空 `tool_use` block（`OutputParserException: Unknown tool type: ''`）—— v0.3.x P3-A 已落地 `retry_on_parser_error`（默认 2 次），仅对 parser 噪声生效
- ⚠️ 没有 `event-adapter` 中文化版本（原 TS 版有）
